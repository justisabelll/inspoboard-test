{
  "name": "@inspoboard/api",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "db:setup": "bun run src/lib/seed.ts --setup",
    "db:seed": "bun run src/lib/seed.ts --seed",
    "db:reset": "bun run src/lib/seed.ts --reset"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.2.2",
    "drizzle-orm": "^0.33.0",
    "hono": "^4.6.2",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "^0.24.2"
  }
}
